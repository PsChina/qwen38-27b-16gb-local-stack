# Copy values into your local launcher configuration. Do not commit real hosts or credentials.
QWEN_PROFILE=Q2
QWEN_MODEL_FILE=Qwen3.8-27B-UD-Q2_K_XL.gguf
QWEN_BACKEND_HOST=127.0.0.1
QWEN_BACKEND_PORT=8080
LLAMA_BACKEND_CONTEXT_TOKENS=182000
CONTEXT_HARD_LIMIT=178000
CONTEXT_SAFETY_TOKENS=4096
AGENT_MAX_CONTEXT_TOKENS=170000

# DeepSeek Harness client key (env name only). The local no-auth endpoint accepts
# the dummy value "local"; never set a real key here.
# QWEN38_API_KEY=local
